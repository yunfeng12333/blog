---
title: 三条居留路径的对比（未完成）
slug: draft-visa-routes
section: move
date: 2026-09-21
summary: 这是一篇草稿，用来验证 draft 文章不会被构建、不会进 sitemap、不会被搜索到。
tags: [身份]
draft: true
---

这篇文章的 frontmatter 里有 `draft: true`。

验证方法：跑 `npm run build` 之后，在 `dist/` 里应该**找不到** `move/draft-visa-routes/`，`sitemap-index.xml` 里也不应该出现它。本地 `npm run dev` 时可以正常看到，方便边写边预览。

正文还没写完，随手记几个要点：

- 三条路径各自的前置条件
- 时间成本对比
- 家属随行的差异

等信息核实之后再补。
